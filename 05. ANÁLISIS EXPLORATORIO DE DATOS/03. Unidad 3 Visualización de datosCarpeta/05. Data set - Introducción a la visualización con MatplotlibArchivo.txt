{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# 📊 Clase 5 — Introducción a la visualización con Matplotlib\n",
    "## Unidad: Visualización de datos\n",
    "\n",
    "**Situación:** Formas parte del equipo de análisis de una empresa de logística. Se te pide presentar visualmente la evolución semanal de entregas exitosas por zona geográfica. Solo cuentas con los datos en bruto y necesitas una herramienta flexible para graficar.\n",
    "\n",
    "**Objetivos:**\n",
    "- Conocer las características de **Matplotlib** y por qué es fundamental\n",
    "- Importar `matplotlib.pyplot` correctamente con el alias `plt`\n",
    "- Crear gráficos con la **interfaz procedural** (`plt.*`)\n",
    "- Crear gráficos con la **interfaz orientada a objetos** (`fig, ax`)\n",
    "- Identificar y personalizar los **componentes de un gráfico**: Figure, Axes, Axis, Ticks, Labels, Title, Legend, Grid\n",
    "- Guardar gráficos con `plt.savefig()`"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "---\n",
    "## PARTE 1 — Características de Matplotlib e importación\n",
    "\n",
    "### 1.1 Instalación e importación correcta"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Instalación (si es necesario):\n",
    "# pip install matplotlib\n",
    "\n",
    "# Importación correcta — código exacto de la presentación\n",
    "import matplotlib.pyplot as plt\n",
    "import pandas as pd\n",
    "import numpy as np\n",
    "\n",
    "print('✅ Matplotlib importado correctamente')\n",
    "import matplotlib\n",
    "print(f'Versión: {matplotlib.__version__}')"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### 1.2 Primer gráfico — ejemplo de la presentación"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Código exacto de la presentación — slide 6\n",
    "import matplotlib.pyplot as plt\n",
    "\n",
    "x = [1, 2, 3, 4, 5]\n",
    "y = [10, 20, 25, 30, 40]\n",
    "\n",
    "plt.plot(x, y)\n",
    "plt.title('Ventas semanales')\n",
    "plt.xlabel('Semana')\n",
    "plt.ylabel('Unidades vendidas')\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### 1.3 Estilos disponibles en Matplotlib"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Buena práctica: usar plt.style.use() para coherencia visual\n",
    "estilos_demo = ['default', 'seaborn-v0_8-whitegrid', 'ggplot', 'bmh', 'fivethirtyeight']\n",
    "\n",
    "fig, axes = plt.subplots(1, 5, figsize=(18, 3))\n",
    "fig.suptitle('Estilos disponibles con plt.style.use()', fontweight='bold')\n",
    "\n",
    "x_s = [1, 2, 3, 4, 5]\n",
    "y_s = [10, 20, 25, 30, 40]\n",
    "\n",
    "for ax, estilo in zip(axes, estilos_demo):\n",
    "    with plt.style.context(estilo):\n",
    "        ax.plot(x_s, y_s, marker='o')\n",
    "        ax.set_title(f'style:\\n\"{estilo}\"', fontsize=7)\n",
    "        ax.set_xlabel('X')\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.show()\n",
    "\n",
    "print('Uso: plt.style.use(\"seaborn-v0_8-whitegrid\")')\n",
    "print('Ver todos los estilos disponibles:')\n",
    "print(plt.style.available[:10], '...')"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### 1.4 Tabla de comandos básicos de pyplot"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Tabla de la presentación — slide 12\n",
    "tabla_pyplot = pd.DataFrame({\n",
    "    'Función': ['plot()','bar()','barh()','scatter()','hist()',\n",
    "                'title()','xlabel(), ylabel()'],\n",
    "    'Tipo de gráfico': ['Línea','Barras verticales','Barras horizontales',\n",
    "                        'Dispersión','Histograma',\n",
    "                        'Título del gráfico','Etiquetas de ejes'],\n",
    "    'Aplicación típica': [\n",
    "        'Tendencias en el tiempo (series temporales)',\n",
    "        'Comparar cantidades por categoría',\n",
    "        'Útil para etiquetas largas o categorías numerosas',\n",
    "        'Visualizar relaciones entre variables numéricas',\n",
    "        'Distribución de frecuencias',\n",
    "        'Identificación del propósito del gráfico',\n",
    "        'Contextualización del eje X e Y'\n",
    "    ]\n",
    "})\n",
    "print(tabla_pyplot.to_string(index=False))"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "---\n",
    "## PARTE 2 — Interfaz procedural vs Orientada a Objetos\n",
    "\n",
    "### 2.1 Interfaz procedural (rápida, para tareas simples)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Código exacto de la presentación — slide 10 (gráfico de barras)\n",
    "import matplotlib.pyplot as plt\n",
    "\n",
    "meses   = ['Ene', 'Feb', 'Mar', 'Abr']\n",
    "ventas  = [100, 120, 90, 150]\n",
    "\n",
    "plt.bar(meses, ventas)\n",
    "plt.title('Ventas por mes')\n",
    "plt.xlabel('Mes')\n",
    "plt.ylabel('Monto en miles')\n",
    "plt.grid(True)\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### 2.2 Interfaz orientada a objetos (recomendada — control total)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Código exacto de la presentación — slide 18\n",
    "import matplotlib.pyplot as plt\n",
    "\n",
    "# Crear figura y eje\n",
    "fig, ax = plt.subplots()\n",
    "\n",
    "# Trazar línea\n",
    "ax.plot([1, 2, 3, 4], [10, 20, 25, 30], label='Ventas')\n",
    "\n",
    "# Personalización del gráfico\n",
    "ax.set_title('Evolución de ventas')\n",
    "ax.set_xlabel('Trimestre')\n",
    "ax.set_ylabel('Miles de unidades')\n",
    "ax.legend()\n",
    "ax.grid(True)\n",
    "\n",
    "# Mostrar figura\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Comparación directa: procedural vs OOP\n",
    "fig, axes = plt.subplots(1, 2, figsize=(13, 4))\n",
    "fig.suptitle('Interfaz Procedural vs Orientada a Objetos', fontweight='bold')\n",
    "\n",
    "x = [1, 2, 3, 4]\n",
    "y = [10, 20, 25, 30]\n",
    "\n",
    "# ── Interfaz procedural (izquierda) ──\n",
    "plt.sca(axes[0])       # seleccionar el eje\n",
    "plt.plot(x, y, 'b-o')\n",
    "plt.title('Interfaz procedural\\nplt.plot(), plt.title()...')\n",
    "plt.xlabel('Trimestre')\n",
    "plt.ylabel('Miles')\n",
    "plt.grid(True)\n",
    "\n",
    "# ── Interfaz OOP (derecha) ──\n",
    "axes[1].plot(x, y, 'r-s', label='Ventas')\n",
    "axes[1].set_title('Interfaz OOP\\nfig, ax = plt.subplots()')\n",
    "axes[1].set_xlabel('Trimestre')\n",
    "axes[1].set_ylabel('Miles')\n",
    "axes[1].legend()\n",
    "axes[1].grid(True)\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.show()\n",
    "\n",
    "print('Recomendación: Usa fig, ax = plt.subplots() para proyectos con múltiples gráficos.')"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### ✏️ Ejercicio 2:"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# ✏️ ¿Cuándo es preferible la interfaz OOP sobre la procedural?\n",
    "r_oop = \"\"\n",
    "\n",
    "# ✏️ ¿Qué error ocurre si plt.plot() y plt.bar() reciben listas de distinta longitud?\n",
    "r_longitud = \"\"\n",
    "\n",
    "# ✏️ ¿Por qué se recomienda usar el alias plt?\n",
    "r_alias = \"\"\n",
    "\n",
    "print(f'OOP preferible: {r_oop}')\n",
    "print(f'Longitud distinta: {r_longitud}')\n",
    "print(f'Alias plt: {r_alias}')"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "---\n",
    "## PARTE 3 — Anatomía de un gráfico en Matplotlib\n",
    "\n",
    "### 3.1 Los componentes clave"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Tabla anatómica de la presentación — slide 17\n",
    "tabla_anat = pd.DataFrame({\n",
    "    'Elemento': ['Figure','Axes','Axis','Tick','Label','Title','Legend','Grid'],\n",
    "    'Descripción': [\n",
    "        'Objeto contenedor global. Puede incluir varios ejes (Axes).',\n",
    "        'Área de trazado donde se colocan datos, etiquetas, título, etc.',\n",
    "        'Los ejes X o Y en sí mismos, contienen ticks y valores numéricos.',\n",
    "        'Marcadores en los ejes, indican valores individuales.',\n",
    "        'Etiquetas asociadas a cada eje (xlabel, ylabel).',\n",
    "        'Título general del gráfico o por cada Axes.',\n",
    "        'Explicación de lo que representa cada serie de datos.',\n",
    "        'Líneas auxiliares que mejoran la legibilidad numérica.'\n",
    "    ]\n",
    "})\n",
    "print(tabla_anat.to_string(index=False))"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Gráfico anotado que muestra cada componente visualmente\n",
    "fig, ax = plt.subplots(figsize=(10, 6))\n",
    "\n",
    "x = [1, 2, 3, 4, 5]\n",
    "y1 = [10, 20, 25, 30, 40]\n",
    "y2 = [8, 15, 22, 28, 35]\n",
    "\n",
    "ax.plot(x, y1, 'b-o', linewidth=2, label='Serie A')\n",
    "ax.plot(x, y2, 'r--s', linewidth=2, label='Serie B')\n",
    "\n",
    "# ── COMPONENTES ANOTADOS ──\n",
    "ax.set_title('TITLE: Título del gráfico', fontsize=13, fontweight='bold', color='darkblue')\n",
    "ax.set_xlabel('LABEL (xlabel): Etiqueta eje X', fontsize=10, color='purple')\n",
    "ax.set_ylabel('LABEL (ylabel): Etiqueta eje Y', fontsize=10, color='purple')\n",
    "ax.legend(title='LEGEND', fontsize=9, title_fontsize=9)\n",
    "ax.grid(True, linestyle='--', alpha=0.5)\n",
    "\n",
    "# Anotaciones\n",
    "ax.annotate('← AXES (área de dibujo)', xy=(3, 22), xytext=(3.5, 15),\n",
    "            arrowprops=dict(arrowstyle='->', color='green'),\n",
    "            color='green', fontsize=9)\n",
    "ax.annotate('TICK →', xy=(2, 0), xytext=(2.2, -3),\n",
    "            fontsize=8, color='brown')\n",
    "\n",
    "ax.text(0.02, 0.98, 'FIGURE (contenedor global)',\n",
    "        transform=fig.transFigure, fontsize=8, color='gray',\n",
    "        verticalalignment='top')\n",
    "\n",
    "# Borde de la figura\n",
    "for spine in ['top','right','bottom','left']:\n",
    "    ax.spines[spine].set_linewidth(2)\n",
    "    ax.spines[spine].set_edgecolor('#CCCCCC')\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### 3.2 Tabla de métodos de personalización (slide 19)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "tabla_metodos = pd.DataFrame({\n",
    "    'Método': ['ax.set_title()','ax.set_xlabel()','ax.set_ylabel()',\n",
    "               'ax.grid(True)','ax.legend()','fig.set_size_inches()','plt.tight_layout()'],\n",
    "    'Descripción': [\n",
    "        'Agrega un título a los ejes',\n",
    "        'Asigna etiqueta al eje X',\n",
    "        'Asigna etiqueta al eje Y',\n",
    "        'Activa la cuadrícula',\n",
    "        'Muestra la leyenda si se ha definido un label',\n",
    "        'Ajusta el tamaño general del gráfico',\n",
    "        'Optimiza la distribución de elementos en la figura'\n",
    "    ]\n",
    "})\n",
    "print(tabla_metodos.to_string(index=False))"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "---\n",
    "## PARTE 4 — Galería de tipos de gráficos con pyplot\n",
    "\n",
    "### 4.1 Los 5 tipos básicos"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "np.random.seed(42)\n",
    "\n",
    "fig, axes = plt.subplots(2, 3, figsize=(16, 9))\n",
    "fig.suptitle('Galería de gráficos básicos en Matplotlib', fontweight='bold', fontsize=13)\n",
    "\n",
    "# 1. plot() — línea\n",
    "x = [1, 2, 3, 4, 5]\n",
    "axes[0,0].plot(x, [10,20,25,30,40], 'b-o', linewidth=2, label='Serie A')\n",
    "axes[0,0].plot(x, [15,18,22,26,35], 'r--s', linewidth=2, label='Serie B')\n",
    "axes[0,0].set_title('plot() — Línea')\n",
    "axes[0,0].set_xlabel('Semana')\n",
    "axes[0,0].set_ylabel('Unidades')\n",
    "axes[0,0].legend()\n",
    "axes[0,0].grid(True)\n",
    "\n",
    "# 2. bar() — barras verticales\n",
    "meses  = ['Ene','Feb','Mar','Abr']\n",
    "ventas = [100, 120, 90, 150]\n",
    "axes[0,1].bar(meses, ventas, color='#2E75B6', edgecolor='white')\n",
    "axes[0,1].set_title('bar() — Barras verticales')\n",
    "axes[0,1].set_xlabel('Mes')\n",
    "axes[0,1].set_ylabel('Monto en miles')\n",
    "axes[0,1].grid(True, axis='y')\n",
    "\n",
    "# 3. barh() — barras horizontales\n",
    "regiones = ['Norte','Sur','Centro','Este','Oeste']\n",
    "valores  = [45, 38, 62, 55, 41]\n",
    "axes[0,2].barh(regiones, valores, color='#70AD47', edgecolor='white')\n",
    "axes[0,2].set_title('barh() — Barras horizontales')\n",
    "axes[0,2].set_xlabel('Entregas')\n",
    "axes[0,2].grid(True, axis='x')\n",
    "\n",
    "# 4. scatter() — dispersión\n",
    "x_s = np.random.randint(10, 100, 50)\n",
    "y_s = x_s * 0.8 + np.random.normal(0, 10, 50)\n",
    "axes[1,0].scatter(x_s, y_s, color='#ED7D31', alpha=0.6, s=40)\n",
    "axes[1,0].set_title('scatter() — Dispersión')\n",
    "axes[1,0].set_xlabel('Variable X')\n",
    "axes[1,0].set_ylabel('Variable Y')\n",
    "axes[1,0].grid(True)\n",
    "\n",
    "# 5. hist() — histograma\n",
    "datos = np.random.normal(50, 12, 200)\n",
    "axes[1,1].hist(datos, bins=15, color='#7030A0', edgecolor='white', alpha=0.8)\n",
    "axes[1,1].set_title('hist() — Histograma')\n",
    "axes[1,1].set_xlabel('Valor')\n",
    "axes[1,1].set_ylabel('Frecuencia')\n",
    "axes[1,1].grid(True, axis='y')\n",
    "\n",
    "# 6. pie() — torta\n",
    "labels_p = ['Zona A','Zona B','Zona C','Zona D']\n",
    "sizes_p  = [35, 25, 20, 20]\n",
    "axes[1,2].pie(sizes_p, labels=labels_p, autopct='%1.1f%%',\n",
    "              startangle=90, colors=['#2E75B6','#ED7D31','#70AD47','#FFC000'])\n",
    "axes[1,2].set_title('pie() — Gráfico de torta')\n",
    "\n",
    "plt.tight_layout()\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### ✏️ Ejercicio 4:"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# ✏️ Asigna el tipo de gráfico correcto a cada escenario:\n",
    "escenarios = {\n",
    "    'Evolución de ventas de un producto durante 12 meses':    '',\n",
    "    'Comparar ingresos de 5 regiones en un mes':              '',\n",
    "    '¿Cómo se distribuyen los tiempos de entrega?':          '',\n",
    "    'Relación entre publicidad y ventas':                     '',\n",
    "    'Porcentaje de cada producto en el total de ventas':      '',\n",
    "    'Comparar muchas ciudades con nombres largos':            '',\n",
    "}\n",
    "\n",
    "print('--- SELECCIÓN DE TIPO DE GRÁFICO ---')\n",
    "for escenario, resp in escenarios.items():\n",
    "    print(f'\\n{escenario}')\n",
    "    print(f'  Función elegida: {resp}')"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "---\n",
    "## PARTE 5 — Actividad guiada: Entregas semanales por zona\n",
    "\n",
    "### Paso 1 — Carga las bibliotecas (código exacto slide 26)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "import matplotlib.pyplot as plt\n",
    "import pandas as pd"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Paso 2 — Crea los datos (código exacto slide 26)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "semanas             = ['Semana 1', 'Semana 2', 'Semana 3', 'Semana 4']\n",
    "entregas_zona_norte = [120, 135, 150, 160]\n",
    "entregas_zona_sur   = [100, 110, 95, 130]"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Paso 3 — Genera la visualización principal (código exacto slide 27)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "fig, ax = plt.subplots()\n",
    "ax.plot(semanas, entregas_zona_norte, label='Zona Norte', marker='o')\n",
    "ax.plot(semanas, entregas_zona_sur,   label='Zona Sur',   marker='s')\n",
    "ax.set_title('Entregas semanales por zona')\n",
    "ax.set_xlabel('Semana')\n",
    "ax.set_ylabel('Cantidad de entregas')\n",
    "ax.legend()\n",
    "ax.grid(True)\n",
    "plt.tight_layout()\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Paso 4 — Guarda el gráfico como imagen (código exacto slide 28)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Recrear el gráfico para guardarlo\n",
    "fig, ax = plt.subplots(figsize=(8, 5))\n",
    "ax.plot(semanas, entregas_zona_norte, label='Zona Norte', marker='o',\n",
    "        color='#2E75B6', linewidth=2)\n",
    "ax.plot(semanas, entregas_zona_sur,   label='Zona Sur',   marker='s',\n",
    "        color='#ED7D31', linewidth=2, linestyle='--')\n",
    "ax.set_title('Entregas semanales por zona', fontsize=13, fontweight='bold')\n",
    "ax.set_xlabel('Semana')\n",
    "ax.set_ylabel('Cantidad de entregas')\n",
    "ax.legend()\n",
    "ax.grid(True, linestyle='--', alpha=0.5)\n",
    "plt.tight_layout()\n",
    "\n",
    "# Guardar — código exacto slide 28\n",
    "fig.savefig('entregas_semanales.png', dpi=150, bbox_inches='tight')\n",
    "print('✅ Gráfico guardado como entregas_semanales.png')\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### Paso 5 — Ampliar con datos del slide 25 (4 productos)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Datos exactos del slide 25\n",
    "import matplotlib.pyplot as plt\n",
    "\n",
    "meses       = ['Enero', 'Febrero', 'Marzo']\n",
    "producto_A  = [120, 135, 150]\n",
    "producto_B  = [80,  95,  100]\n",
    "producto_C  = [150, 160, 170]\n",
    "producto_D  = [100, 110, 130]\n",
    "\n",
    "fig, ax = plt.subplots(figsize=(9, 5))\n",
    "\n",
    "ax.plot(meses, producto_A, marker='o', linewidth=2, label='Producto A', color='#2E75B6')\n",
    "ax.plot(meses, producto_B, marker='s', linewidth=2, label='Producto B', color='#ED7D31')\n",
    "ax.plot(meses, producto_C, marker='^', linewidth=2, label='Producto C', color='#70AD47')\n",
    "ax.plot(meses, producto_D, marker='D', linewidth=2, label='Producto D', color='#7030A0')\n",
    "\n",
    "ax.set_title('Ventas mensuales por producto — Q1', fontsize=13, fontweight='bold')\n",
    "ax.set_xlabel('Mes')\n",
    "ax.set_ylabel('Ventas (unidades)')\n",
    "ax.legend(title='Producto')\n",
    "ax.grid(True, linestyle='--', alpha=0.5)\n",
    "plt.tight_layout()\n",
    "plt.show()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "### ✏️ Preguntas de reflexión de la actividad guiada:"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# ✏️ 1. ¿Qué diferencia hay entre marker='o' y marker='s'?\n",
    "c1 = \"\"\n",
    "\n",
    "# ✏️ 2. ¿Por qué es importante usar ax.legend() cuando hay varias series?\n",
    "c2 = \"\"\n",
    "\n",
    "# ✏️ 3. ¿Qué ventaja tiene fig.savefig() frente a captura de pantalla?\n",
    "c3 = \"\"\n",
    "\n",
    "# ✏️ 4. ¿Qué significa tight_layout() y cuándo es necesario?\n",
    "c4 = \"\"\n",
    "\n",
    "# ✏️ 5. ¿Qué producto tuvo mejor desempeño? ¿Cómo lo lees en el gráfico?\n",
    "c5 = \"\"\n",
    "\n",
    "print('--- REFLEXIONES ---')\n",
    "for i, c in enumerate([c1, c2, c3, c4, c5], 1):\n",
    "    print(f'{i}. {c}')"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "---\n",
    "## 📋 Resumen de componentes y buenas prácticas\n",
    "\n",
    "### Anatomía del gráfico\n",
    "\n",
    "| Componente | Descripción | Método OOP |\n",
    "|-----------|-------------|------------|\n",
    "| `Figure` | Contenedor global de la visualización | `fig, ax = plt.subplots()` |\n",
    "| `Axes` | Área de trazado (¡no confundir con Axis!) | `ax = ...` |\n",
    "| `Axis` | Los ejes X e Y con ticks y valores | `ax.xaxis`, `ax.yaxis` |\n",
    "| `Title` | Título del gráfico | `ax.set_title('...')` |\n",
    "| `Label` | Etiquetas de ejes | `ax.set_xlabel('...')` |\n",
    "| `Legend` | Leyenda para identificar series | `ax.legend()` |\n",
    "| `Grid` | Cuadrícula de referencia | `ax.grid(True)` |\n",
    "\n",
    "### Procedural vs OOP\n",
    "\n",
    "| | Procedural (`plt.*`) | OOP (`fig, ax`) |\n",
    "|-|---------------------|-------------------|\n",
    "| Sintaxis | `plt.plot()`, `plt.title()` | `ax.plot()`, `ax.set_title()` |\n",
    "| Cuándo usar | Scripts rápidos, exploratorios | Proyectos con múltiples subgráficos |\n",
    "| Control | Limitado | Total |\n",
    "| Recomendado para | Aprendizaje inicial | Producción |\n",
    "\n",
    "> 💡 **`fig, ax = plt.subplots()`** es la forma recomendada para tener control total. Úsala siempre en proyectos que van más allá de un gráfico rápido.\n",
    "\n",
    "> 💡 **`axes` ≠ `axis`:** `axes` es el área de dibujo (el subplot completo). `axis` son las líneas X o Y con sus ticks. Confundirlos es uno de los errores más comunes."
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.10.0"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}